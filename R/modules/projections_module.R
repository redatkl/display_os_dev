# Page 1 Module: Dashboard

projections_ui <- function(id) {
  ns <- NS(id)
  
 
  tags$h2("Projections Page - Under Construction", class = "page-title")
}

projections_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    
    
  })
}