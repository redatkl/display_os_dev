# Page 2 Module: Data Analysis

data_ui <- function(id) {
  ns <- NS(id)
  
    tags$h2("Data Analysis", class = "page-title")
}

data_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    

  })
}