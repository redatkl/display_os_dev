# Page 2 Module: Data

data_ui <- function(id) {
  ns <- NS(id)
  
  tags$h2("Data Page - Under Construction", class = "page-title")
}

data_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    

  })
}