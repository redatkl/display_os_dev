#  Module: Contact

contact_ui <- function(id) {
  ns <- NS(id)
  
 
  tags$h2("Contact Page - Under Construction", class = "page-title")
}

contact_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    
    
  })
}