# Page Forecast

forecast_ui <- function(id) {
  ns <- NS(id)
  
  tags$h2("Forecast Page - Under Construction", class = "page-title")
}

forecast_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    
    
  })
}